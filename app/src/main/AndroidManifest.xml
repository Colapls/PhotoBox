<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".PhotoBoxApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PhotoBox">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.PhotoBox">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- 微信 SDK 回调入口（必须 export=false，由微信进程启动） -->
        <activity
            android:name=".wechat.WXEntryActivity"
            android:exported="false"
            android:taskAffinity="com.photobox.wxapi"
            android:launchMode="singleTask"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="wx0000000000000000" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="WX_APP_ID"
            android:value="wx0000000000000000" />
    </application>

</manifest>
